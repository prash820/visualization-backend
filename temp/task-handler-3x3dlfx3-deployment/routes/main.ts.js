import express from "express"; import cors from "./middleware/cors"; import apiRoutes from "./api"; import healthRoutes from "./health"; import { config } from "./config/environment"; const app = express(); app.use(cors); app.use(express.json()); app.use("/api", apiRoutes); app.use("/health", healthRoutes); const PORT = config.PORT || 3000; app.listen(PORT, () => { console.log(`Server running on port ${PORT}`); });